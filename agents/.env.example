PORT=4100

# Shared secret required on X-Internal-Secret for every /internal/* request
# when set. Unset in local dev — must match backend's AGENTS_SHARED_SECRET
# in any deployment reachable by more than just the backend (this service
# has no other auth of its own by design; see agents/src/index.ts).
AGENTS_SHARED_SECRET=
