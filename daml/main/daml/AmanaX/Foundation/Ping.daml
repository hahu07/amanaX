module AmanaX.Foundation.Ping where

-- | Milestone 0 scaffold-proving template. Deleted once the real Identity
-- module (Organization/User, Milestone 1) lands.
template Ping
  with
    owner : Party
  where
    signatory owner

    choice Pong : ()
      controller owner
      do
        pure ()
